LEAST-PRIVILEGE ACCESS REVIEW WORKBENCH — SPOKEN NARRATION
Planned finished runtime 2:36. Timed caption cues live in demo-captions.srt.
Delivery: conversational, confident, unhurried on the two safety beats.

Alex Morgan's contract ended Friday. It's Tuesday, and Alex can still ship code to production and export customer data.

Nobody has noticed, because that access isn't on Alex's profile. It's inherited.

Every offboarding team knows this problem. The truth is buried in an access console nobody has time to read.

And pasting screenshots into a chat window gets you a guess, not a path you can defend.

So this page hands the agent real tools instead of pixels. Nine WebMCP tools, registered right in the document.

Resolve. Trace. Simulate. Preview. Stage. Codex discovers all nine, and one sentence starts the review.

From here I don't touch the interface. Codex resolves the identity, computes effective access, and the map redraws itself.

Four separate paths. Six sensitive grants. Each one explained, not just listed.

The interesting one is the second. Alex sits in Contractors, Contractors nests into Platform Contributors,

and that grants Release Manager, which approves production releases.

The membership graph even loops back on itself. The traversal catches the cycle instead of counting it twice.

Now the part a checklist gets wrong. Codex simulates removing everything, and the page pushes back.

On-call Incident Relay depends on Alex's rollback grant. Revoke it all, and the next outage has no way back.

So the plan gets revised, not abandoned. Five grants leave Alex. One shared dependency moves to a service identity.

Exposure falls from ninety-two to eighteen. And so far, nothing has actually changed.

Because the agent can't change it. Staging is a tool. Confirming is not.

Codex asks for the receipt, and the page answers plainly: no receipt exists yet.

The last step is mine. One human click, one read-only receipt.

Then Codex re-reads the live state: Alex, zero sensitive grants. Rollback now owned by the on-call service identity.

That's what WebMCP unlocks. An agent reasoning inside your app, on your data model, with the decision still human.

The identities here are synthetic. The offboarding problem is not.
